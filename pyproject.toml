[project]
name = "flowboard"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "flask==3.1.2",
    "gunicorn==21.2.0",
    "Werkzeug==3.1.3",
    "libsql-experimental==0.0.35"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
